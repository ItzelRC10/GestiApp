@page
@model IndexModel
@{
    var saludo = Model.ObtenerSaludo();
}

<div class="container mt-5">
    <h1 class="display-4 text-center">@saludo</h1>
    <p class="lead text-center">Bienvenido a tu sistema de gestión de tareas.</p>
    <p class="text-center">Aquí puedes organizar tus actividades, completar tareas pendientes y mantener un registro de tu progreso.</p>

    <div class="d-flex justify-content-center">
        <a asp-page="/Listado" class="btn btn-primary btn-lg mx-2">Ver Tareas</a>
        <a asp-page="/Agregar" class="btn btn-success btn-lg mx-2">Agregar Tarea</a>
    </div>
    <div class="d-flex justify-content-center">
        <img src="/images/escritorio.png" class="img-fluid">
    </div>
</div>